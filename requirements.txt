# Core dependencies (required)
numpy>=1.24.0,<2.0.0
opencv-python>=4.5
imageio>=2.30
imageio-ffmpeg>=0.4
tqdm>=4.60
scikit-image>=0.19

# ONNX runtime backend (required). Falls back to OpenCV DNN if missing.
onnx>=1.14
# 不要硬钉版本：需与显卡/CUDA 匹配。
#   - 老卡 + CUDA 11.8：onnxruntime-gpu 1.16.x 可用
#   - RTX 50 系(Blackwell, sm_120)：必须 CUDA 12.8+ 构建，用最新版 onnxruntime-gpu
# 用 >= 避免 pip install -r 把已装的较新版本强行降级。
onnxruntime-gpu>=1.16.3

# GPU management for multi-GPU support
nvidia-ml-py3>=7.352.0

# Web UI —— 钉到已验证可用的版本栈。
# 教训：只钉 gradio 不钉 starlette/fastapi 会版本错位（旧 gradio + 新 starlette
# 导致 jinja2 "unhashable type: dict" / "localhost not accessible" 崩溃），故整套钉死。
gradio==6.18.0
gradio_client==2.5.0
fastapi==0.136.3
starlette==1.3.1
huggingface_hub==1.19.0
